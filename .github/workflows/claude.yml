name: claude

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  pipeline:
    uses: MaciejMajchrzak/CI.Infra/.github/workflows/claude-service.yml@main
    permissions:
      contents: read
      packages: write
    with:
      project-name: ci-platform-notifications
      api-project: src/CI.Platform.Notifications.API/CI.Platform.Notifications.API.csproj
    secrets: inherit
